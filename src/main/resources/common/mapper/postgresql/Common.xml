<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
         "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.x2bee.common.app.repository.CommonMapper">

    <select id="getSequenceNextVal" resultType="long">
		SELECT 1 from dual
    </select>

    <select id="getSequenceCurrentVal" resultType="long">
		SELECT 1 from dual
    </select>

    <select id="getSysdate" resultType="java.sql.Timestamp">
		SELECT 1 from dual
    </select>

</mapper>